<?php
    // Indexed array
    $people = array('Kevin', 'Dennis', 'Jeremy');
    $ids = array(101,123,155);
    $cars = ['Ford', 'Volkswagen', 'Honda'];
    array_push($cars, 'BMW');
    count($cars);

    // Associative arrays
    $newArray = array('Brad' => 35, 'Jose' => 32, 'William' => 25);
    echo $newArray['Brad'];

    // Debugging array functions
    // var_dump();
    // print_r();
?>